#AI generated manifest
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-a
  namespace: cdn-poc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-a
  template:
    metadata:
      labels:
        app: service-a
    spec:
      containers:
      - name: app
        image: hashicorp/http-echo
        args:
        - "-text=response-from-service-a"
        ports:
        - containerPort: 5678

---
apiVersion: v1
kind: Service
metadata:
  name: service-a
  namespace: cdn-poc
spec:
  selector:
    app: service-a
  ports:
  - port: 80
    targetPort: 5678

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-b
  namespace: cdn-poc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-b
  template:
    metadata:
      labels:
        app: service-b
    spec:
      containers:
      - name: app
        image: hashicorp/http-echo
        args:
        - "-text=response-from-service-b"
        ports:
        - containerPort: 5678

---
apiVersion: v1
kind: Service
metadata:
  name: service-b
  namespace: cdn-poc
spec:
  selector:
    app: service-b
  ports:
  - port: 80
    targetPort: 5678