#ai generated
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: service-login
  namespace: cdn-v2-poc
spec:
  hosts:
  - api.poc.local

  gateways:
  - cdn-poc/cdn-poc-gateway

  http:
  - match:
        - uri:
            prefix: /api/login
    route:
    - destination:
        host: login
        port:
          number: 80