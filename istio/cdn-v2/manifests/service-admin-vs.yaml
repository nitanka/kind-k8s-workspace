#ai generated
apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: service-admin
  namespace: cdn-v2-poc
spec:
  hosts:
  - api.poc.local

  gateways:
  - cdn-poc/cdn-poc-gateway

  http:
  - match:
      - uri:
          prefix: /api/admin
    route:
    - destination:
        host: admin
        port:
          number: 80